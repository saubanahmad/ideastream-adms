/** src/components/CommentBox.jsx — Comment Input + List — Implementation: Phase 3 */
const CommentBox = ({ postId, comments = [] }) => (
  <div className="mt-3 border-t border-brand-primary/40 pt-3">
    <p className="text-brand-cream/50 text-sm">Comments coming in Phase 3</p>
  </div>
);
export default CommentBox;
