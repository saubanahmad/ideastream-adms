/** src/components/CreatePost.jsx — Create Post Form — Implementation: Phase 3 */
const CreatePost = ({ onPost }) => (
  <div className="card mb-6">
    <p className="text-brand-cream/70 font-display text-lg">
      💡 Share your idea... <span className="text-xs">(Coming in Phase 3)</span>
    </p>
  </div>
);
export default CreatePost;
